package com.mycontact.contact.command;

import com.mycontact.user.command.Command;

/**
 * Marker interface for contact-related commands.
 * Extends the generic Command interface for reuse.
 */
public interface ContactCommand extends Command {
}
